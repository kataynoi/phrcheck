[www]
; ค่าเริ่มต้นของ PHP-FPM คือ clear_env = yes ซึ่งจะล้าง environment ทิ้ง
; ทำให้ getenv('DB_HOST') / getenv('LINE_CHANNEL_ID') ใน worker คืน false
; ทั้งที่ docker-compose ส่งค่าเข้ามาแล้ว
clear_env = no
